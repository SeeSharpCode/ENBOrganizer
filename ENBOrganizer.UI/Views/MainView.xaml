<Window x:Class="ENBOrganizer.UI.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Views="clr-namespace:ENBOrganizer.UI.Views"
        xmlns:ViewModels="clr-namespace:ENBOrganizer.UI.ViewModels"
        Title="ENB Organizer" SizeToContent="WidthAndHeight">
    <Window.DataContext>
        <ViewModels:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
            <Views:ActiveGameView />
            <Button Name="ManageGamesButton" Content="Manage Games" Command="{Binding ManageGamesCommand, Mode=OneWay}" Margin="5,0,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" />
        </StackPanel>

        <Views:PresetsView Grid.Row="1" Grid.Column="0" />

        <Views:MasterListView Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" />
    </Grid>
</Window>

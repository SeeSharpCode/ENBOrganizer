<UserControl x:Class="ENBOrganizer.UI.Views.MasterListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ENBOrganizer.UI.Views"
             xmlns:ViewModels="clr-namespace:ENBOrganizer.UI.ViewModels"
             xmlns:toolkitLayout="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Layout.Toolkit"
             mc:Ignorable="d" 
             Background="{StaticResource BackgroundKey}">
    <UserControl.DataContext>
        <ViewModels:MasterListViewModel />
    </UserControl.DataContext>
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Master List" Style="{StaticResource HeaderTextBlockStyle}" />
            <StackPanel Orientation="Horizontal">
                <Button Content="Add..." Command="{Binding AddMasterListItemCommand}" />
                <Button Content="Delete" Command="{Binding DeleteMasterListItemCommand}" />
            </StackPanel>
            <DataGrid ItemsSource="{Binding MasterListItems, Mode=OneWay}" SelectedItem="{Binding SelectedMasterListItem}" IsReadOnly="True" GridLinesVisibility="Horizontal" />
        </StackPanel>
    </Grid>
</UserControl>

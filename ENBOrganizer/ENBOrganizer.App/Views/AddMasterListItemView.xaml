<UserControl x:Class="ENBOrganizer.App.Views.AddMasterListItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:ENBOrganizer.App.Views"
             mc:Ignorable="d"
             DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=AddMasterListItemViewModel}">
    <StackPanel Orientation="Vertical" Margin="10">
        <Label Content="Name" Padding="0" FontWeight="Medium" />
        <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinWidth="100" />
        <Label Content="Type" FontWeight="Medium" Padding="0" Margin="0 10 0 0"/>
        <ComboBox ItemsSource="{Binding MasterListItemTypes, Mode=OneTime}" 
                  SelectedItem="{Binding SelectedMasterListItemType}" IsSynchronizedWithCurrentItem="True" />

        <StackPanel Orientation="Horizontal" Margin="0 5 0 0">
            <Button Content="SAVE" Command="{Binding SaveCommand, Mode=OneTime}" 
                    Style="{StaticResource MaterialDesignFlatButton}" />
            <Button Content="CANCEL" Command="{Binding CancelCommand, Mode=OneTime}" 
                    Style="{StaticResource MaterialDesignFlatButton}" />
        </StackPanel>
    </StackPanel>
</UserControl>

<Window x:Class="ENBOrganizer.UI.Views.AddMasterListItemView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ENBOrganizer.UI.Views"
        xmlns:ViewModels="clr-namespace:ENBOrganizer.UI.ViewModels"
        mc:Ignorable="d">
    <Window.DataContext>
        <ViewModels:AddMasterListItemViewModel  />
    </Window.DataContext>
    <Grid>
        <StackPanel Orientation="Vertical">
            <Label>Enter Name:</Label>
            <TextBox Text="{Binding Name, Mode=OneWayToSource}" />
            <ComboBox ItemsSource="{Binding MasterListItemTypes}"  DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding Type, Mode=TwoWay}" />
            <Button Content="Add" Command="{Binding AddMasterListItemCommand}" />
        </StackPanel>
    </Grid>
</Window>

<UserControl x:Class="ENBOrganizer.App.Views.AddMasterListItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:ENBOrganizer.App.Views"
             mc:Ignorable="d">
    <StackPanel Orientation="Vertical" Margin="10">
        <Label Content="Name" />
        <TextBox Text="{Binding Name, Mode=TwoWay}" MinWidth="100" />
        <Label Content="Type" />
        <ComboBox ItemsSource="{Binding MasterListItemTypes, Mode=OneTime}" SelectedItem="{Binding SelectedMasterListItemType}" IsSynchronizedWithCurrentItem="True" />
        <Button Command="{Binding AddMasterListItemCommand, Mode=OneTime}" HorizontalAlignment="Right">
            <TextBlock Text="Save" />
        </Button>
    </StackPanel>
</UserControl>
